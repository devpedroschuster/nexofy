# PED-90 a PED-96: Correções de Bugs (Super Admin + Painel do Estúdio) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir os 7 bugs reportados nos tickets PED-90 a PED-96 (validação de WhatsApp, checagem de slug duplicado, acentuação, responsividade mobile em duas telas, label de menu em inglês e vazamento de valor sentinela na UI), num único worktree/branch, entregues numa única PR ao final.

**Architecture:** Cada task é uma correção isolada e independentemente testável/verificável em um arquivo (ou par de arquivos) já existente — sem novas abstrações, sem mudança de arquitetura. Onde há lógica pura (validação de telefone, pluralização de terminologia), a correção ganha teste unitário (vitest) seguindo o padrão já usado no repo (`src/lib/*.test.js`). Onde o bug é puramente visual/responsivo (Tailwind), a verificação é manual via browser em viewport mobile (375×812) mais checagem de que a suíte de testes existente continua verde.

**Tech Stack:** React 19, Vite, TanStack Query, Tailwind CSS v4, Supabase JS client, Vitest.

**Spec:** Tickets Linear PED-90, PED-91, PED-92, PED-93, PED-94, PED-95, PED-96 (workspace "Pedro Schuster"). Não há doc de design separado — os tickets já têm passos de reprodução e sugestão de correção; este plano os transcreve para cada task.

## Global Constraints

- Não alterar `ModalCriarEstudio.jsx` (componente morto, não importado por ninguém) — fora de escopo dos tickets; será reportado como issue separada, não corrigido aqui.
- Não introduzir nova biblioteca de máscara/telefone — reusar utilitário já existente (`formatarTelefone` em `src/lib/utils.js`, hoje sem uso) e o padrão de validação já usado em `alunoSchema.telefone` (10 ou 11 dígitos).
- Seguir o padrão visual já estabelecido no repo para tabela responsiva (`hidden md:block` para desktop + `md:hidden` para cards mobile, visto em `src/pages/Professor/ProfessorAlunos.jsx`).
- Todas as strings de UI corrigidas devem usar acentuação correta em português (pt-BR).
- Rodar `npm test` (vitest) e `npm run lint` (eslint) antes de cada commit — baseline atual: 84 testes passando, 9 arquivos de teste.
- Todos os commits e a PR final em português, seguindo o estilo dos commits recentes do repo (`fix: ...`, `docs: ...`).

---

### Task 1: PED-90 — Validação de WhatsApp no cadastro de novo estúdio

**Files:**
- Modify: `webapp/src/lib/utils.js` (adicionar `validarTelefone`)
- Create: `webapp/src/lib/utils.test.js`
- Modify: `webapp/src/pages/SuperAdmin/pages/SuperAdminNovoEstudio.jsx:1-20,148-196` (import `ErrorMessage`, `formatarTelefone`, `validarTelefone`; novo state `whatsappErro`; novo handler; usar no campo WhatsApp)

**Interfaces:**
- Produces: `validarTelefone(telefone: string) => boolean` — exportada de `src/lib/utils.js`, usada por Task 1 e reaproveitável por qualquer form futuro com campo de telefone.

- [ ] **Step 1: Escrever o teste que falha**

Criar `webapp/src/lib/utils.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { validarTelefone } from './utils';

describe('validarTelefone', () => {
  it('aceita telefone com 10 dígitos (fixo, com DDD)', () => {
    expect(validarTelefone('5133334444')).toBe(true);
  });

  it('aceita telefone com 11 dígitos (celular, com DDD e 9)', () => {
    expect(validarTelefone('51999990000')).toBe(true);
  });

  it('aceita telefone já formatado com máscara', () => {
    expect(validarTelefone('(51) 99999-0000')).toBe(true);
  });

  it('rejeita texto livre sem dígitos suficientes', () => {
    // Regressão (PED-90): campo WhatsApp aceitava "abcXYZ!@#" sem validação.
    expect(validarTelefone('abcXYZ!@#')).toBe(false);
  });

  it('rejeita string vazia', () => {
    expect(validarTelefone('')).toBe(false);
  });

  it('rejeita quantidade de dígitos fora de 10/11', () => {
    expect(validarTelefone('123')).toBe(false);
    expect(validarTelefone('123456789012')).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

Run: `npm test -- utils.test.js` (a partir de `webapp/`)
Expected: FAIL — `validarTelefone` não está exportado de `./utils`.

- [ ] **Step 3: Implementar `validarTelefone`**

Em `webapp/src/lib/utils.js`, logo após `formatarTelefone` (linha ~93, após o `};` de fechamento):

```js
// Mesma regra já usada em alunoSchema.telefone (src/lib/validation.js):
// aceita 10 dígitos (fixo + DDD) ou 11 (celular + DDD), ignorando qualquer
// formatação/máscara já presente no valor.
export const validarTelefone = (telefone) => {
  if (typeof telefone !== 'string') return false;
  const digitos = telefone.replace(/\D/g, '');
  return digitos.length === 10 || digitos.length === 11;
};
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Run: `npm test -- utils.test.js`
Expected: PASS (6 testes)

- [ ] **Step 5: Usar a validação no formulário de novo estúdio**

Em `webapp/src/pages/SuperAdmin/pages/SuperAdminNovoEstudio.jsx`:

Adicionar aos imports (perto da linha 15-17):
```js
import Input, { Label, ErrorMessage } from '../../../components/ui/Input';
import { formatarTelefone, validarTelefone } from '../../../lib/utils';
```

Adicionar novo state, logo após `const [slugManual, setSlugManual] = useState(false);` (linha 49):
```js
const [whatsappErro, setWhatsappErro] = useState('');
```

Adicionar novo handler, logo após `handleSlugChange` (após linha 67):
```js
function handleWhatsappChange(e) {
  const bruto = e.target.value;
  const digitos = bruto.replace(/\D/g, '');
  const formatado = digitos.length >= 10 ? formatarTelefone(digitos) : bruto;
  set('whatsapp', formatado);
  setWhatsappErro(
    digitos.length > 0 && !validarTelefone(digitos)
      ? 'WhatsApp inválido. Use DDD + número (10 ou 11 dígitos).'
      : ''
  );
}
```

Adicionar checagem de bloqueio de submit em `handleSubmit` (linha ~89-96), logo após a checagem de `slugValido`:
```js
function handleSubmit(e) {
  e.preventDefault();
  if (!slugValido) {
    showToast.error('Slug inválido. Use letras minúsculas, números e hífens (3–50 chars).');
    return;
  }
  if (form.whatsapp.trim() && !validarTelefone(form.whatsapp)) {
    showToast.error('WhatsApp inválido. Corrija o campo antes de continuar.');
    return;
  }
  criar();
}
```

Substituir o bloco do campo WhatsApp (linhas 172-183):
```jsx
<div>
  <Label htmlFor={`${uid}-whats`}>WhatsApp</Label>
  <Input
    id={`${uid}-whats`}
    leftIcon={<Phone size={16} />}
    placeholder="51 9 9999-0000"
    value={form.whatsapp}
    onChange={handleWhatsappChange}
    error={!!whatsappErro}
    errorId={`${uid}-whats-error`}
    disabled={isPending}
  />
  <ErrorMessage id={`${uid}-whats-error`}>{whatsappErro}</ErrorMessage>
</div>
```

- [ ] **Step 6: Verificar manualmente no browser**

Rodar `npm run dev`, logar como super-admin, ir em `/super/estudios/novo`, digitar `abcXYZ!@#` no campo WhatsApp — deve aparecer a borda vermelha + mensagem "WhatsApp inválido...". Digitar um número válido (ex: `51999990000`) — deve formatar para `(51) 99999-0000` e o erro deve sumir.

- [ ] **Step 7: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: todos os testes passam (84 + 6 novos = 90), lint sem erros novos.

- [ ] **Step 8: Commit**

```bash
git add webapp/src/lib/utils.js webapp/src/lib/utils.test.js webapp/src/pages/SuperAdmin/pages/SuperAdminNovoEstudio.jsx
git commit -m "fix(super-admin): valida formato do WhatsApp ao criar estudio (PED-90)"
```

---

### Task 2: PED-91 — Checagem de slug duplicado em tempo real

**Files:**
- Modify: `webapp/src/services/superAdminService.js` (adicionar `verificarSlugDisponivel`)
- Modify: `webapp/src/pages/SuperAdmin/pages/SuperAdminNovoEstudio.jsx` (usar `useQuery` + `useDebounce` para checar disponibilidade)

**Interfaces:**
- Consumes: `useDebounce(value, delay)` de `src/hooks/useDebounce.js` (já existe, usado em `TabelaEstudios.jsx`).
- Produces: `superAdminService.verificarSlugDisponivel(slug: string) => Promise<boolean>` (true = disponível).

- [ ] **Step 1: Implementar `verificarSlugDisponivel` no service**

Em `webapp/src/services/superAdminService.js`, adicionar após `criarEstudio` (antes do `export const superAdminService = {`):

```js
/**
 * Verifica se um slug já está em uso por outro estúdio — usado para dar
 * feedback em tempo real no formulário de criação, antes do submit (a
 * Edge Function `criar-estudio` já valida unicidade no backend; isto é
 * só uma checagem antecipada de UX, não substitui a validação server-side).
 */
async function verificarSlugDisponivel(slug) {
  const { data, error } = await supabase
    .from('estudios')
    .select('id')
    .eq('slug', slug)
    .maybeSingle();

  if (error) throw error;
  return !data;
}
```

E adicionar `verificarSlugDisponivel` ao objeto exportado `superAdminService` no final do arquivo.

- [ ] **Step 2: Consumir a checagem no formulário**

Em `webapp/src/pages/SuperAdmin/pages/SuperAdminNovoEstudio.jsx`:

Adicionar aos imports:
```js
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useDebounce } from '../../../hooks/useDebounce';
```

Logo após `const slugTocado = form.slug.length > 0;` (linha 52), adicionar:
```js
const slugDebounced = useDebounce(form.slug, 400);

const { data: slugDisponivel, isFetching: verificandoSlug } = useQuery({
  queryKey: ['super-admin', 'slug-disponivel', slugDebounced],
  queryFn: () => superAdminService.verificarSlugDisponivel(slugDebounced),
  enabled: SLUG_RE.test(slugDebounced),
  staleTime: 1000 * 30,
});

// Só considera "duplicado" quando o slug debounced bate com o slug atual
// do campo — evita mostrar um resultado desatualizado enquanto o usuário
// ainda está digitando um valor diferente do que foi checado.
const slugDuplicado = form.slug === slugDebounced && slugDisponivel === false;
```

Atualizar o `rightIcon` do campo slug (linhas 153-159) para refletir os três estados (verificando / duplicado / válido / inválido):
```jsx
rightIcon={
  slugTocado
    ? !slugValido
      ? <XCircle    size={16} className="text-destructive" />
      : verificandoSlug
        ? <Loader2  size={16} className="animate-spin text-muted-foreground" />
        : slugDuplicado
          ? <XCircle  size={16} className="text-destructive" />
          : <CheckCircle size={16} className="text-success" />
    : null
}
```

Adicionar `Loader2` aos imports de `lucide-react` (linha 10-13).

Adicionar mensagem de erro abaixo do hint existente (após linha 169, dentro do mesmo `<div>` do campo slug):
```jsx
{slugDuplicado && (
  <p className="mt-1 text-[11px] text-destructive font-medium">
    Esse slug já está em uso por outro estúdio.
  </p>
)}
```

Bloquear submit quando duplicado — em `handleSubmit`, adicionar mais uma guarda antes de `criar()`:
```js
if (slugDuplicado) {
  showToast.error('Esse slug já está em uso. Escolha outro.');
  return;
}
```

- [ ] **Step 3: Verificar manualmente no browser**

Rodar `npm run dev`, logar como super-admin, ir em `/super/estudios/novo`, digitar no campo "Nome do estúdio" um nome cujo slug já existe (ex.: `iluminus`, conflitando com "Espaço Iluminus"). Esperado: depois do debounce, o ícone do campo Slug vira X vermelho e aparece "Esse slug já está em uso por outro estúdio." Trocar para um nome com slug livre — o ✓ verde deve voltar.

- [ ] **Step 4: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: sem regressões (o comportamento aqui depende de rede/Supabase, por isso não há teste unitário automatizado para este fluxo — a verificação é manual, Step 3).

- [ ] **Step 5: Commit**

```bash
git add webapp/src/services/superAdminService.js webapp/src/pages/SuperAdmin/pages/SuperAdminNovoEstudio.jsx
git commit -m "fix(super-admin): detecta slug duplicado em tempo real ao criar estudio (PED-91)"
```

---

### Task 3: PED-92 — Acentuação nos cabeçalhos, modais e toasts de Estúdios

**Files:**
- Modify: `webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx` (linhas 75, 236, 274, 279, 292, 296, 325, 365-370)

- [ ] **Step 1: Corrigir as strings sem acento**

Em `webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx`, aplicar as seguintes substituições exatas:

| Linha (atual) | De | Para |
|---|---|---|
| 75 | `aria-label="Acoes"` | `aria-label="Ações"` |
| 236 | `'Estudio suspenso.' : 'Estudio reativado.'` | `'Estúdio suspenso.' : 'Estúdio reativado.'` |
| 274 | `<TH className="pl-6 pr-4 text-left">Estudio</TH>` | `<TH className="pl-6 pr-4 text-left">Estúdio</TH>` |
| 279 | `<TH className="pr-6 pl-4 text-right">Acoes</TH>` | `<TH className="pr-6 pl-4 text-right">Ações</TH>` |
| 292 | `buscaDebounced ? 'Nenhum estudio encontrado' : 'Nenhum estudio cadastrado'` | `buscaDebounced ? 'Nenhum estúdio encontrado' : 'Nenhum estúdio cadastrado'` |
| 296 | `'Crie o primeiro estudio usando o botao acima.'` | `'Crie o primeiro estúdio usando o botão acima.'` |
| 325 | `{totalCount} estudio{totalCount !== 1 ? 's' : ''}` | `{totalCount} estúdio{totalCount !== 1 ? 's' : ''}` |
| 365 | `confirmacao?.acao === 'suspender' ? 'Suspender estudio?' : 'Reativar estudio?'` | `confirmacao?.acao === 'suspender' ? 'Suspender estúdio?' : 'Reativar estúdio?'` |
| 368 | `` `O estudio "${confirmacao?.estudio?.nome}" perdera acesso ao sistema.` `` | `` `O estúdio "${confirmacao?.estudio?.nome}" perderá acesso ao sistema.` `` |
| 369 | `` `O estudio "${confirmacao?.estudio?.nome}" voltara a ter acesso normalmente.` `` | `` `O estúdio "${confirmacao?.estudio?.nome}" voltará a ter acesso normalmente.` `` |

- [ ] **Step 2: Verificar que nenhuma ocorrência sem acento restou**

Run (a partir de `webapp/`): `grep -n "estudio\|Estudio\|Acoes\|perdera\|voltara" src/pages/SuperAdmin/components/TabelaEstudios.jsx`
Expected: nenhuma linha de string de UI restante sem acento (a variável `estudio` em código — `const ativo = estudio.status`, `estudio={e}` etc. — é identificador, não string visível, e não deve ser alterada).

- [ ] **Step 3: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: sem regressões.

- [ ] **Step 4: Verificar manualmente no browser**

Ir em `/super/estudios`: cabeçalhos "ESTÚDIO"/"AÇÕES", rodapé "N estúdios", e nos modais de Suspender/Reativar + toasts resultantes, tudo com acentuação correta.

- [ ] **Step 5: Commit**

```bash
git add webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx
git commit -m "fix(super-admin): corrige acentuacao na tabela e nos modais de estudios (PED-92)"
```

---

### Task 4: PED-93 — Tabela de Estúdios responsiva em mobile

**Files:**
- Modify: `webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx` (adicionar view em cards para mobile, seguindo o padrão de `src/pages/Professor/ProfessorAlunos.jsx:296,415`)

**Interfaces:**
- Consumes: `MenuAcoes`, `LinhaEstudio`, `SkeletonLinha`, `nomeExibicao`, `inicial`, `contagem` — já definidos no próprio arquivo (Task 3 já deixou essas strings corrigidas).

- [ ] **Step 1: Envolver a tabela existente com `hidden md:block`**

No JSX de `TabelaEstudios` (dentro do `return`, por volta da linha 269), trocar:
```jsx
<div className="rounded-3xl border border-border bg-card shadow-card overflow-hidden">
  <div className="overflow-x-auto">
    <table className="w-full min-w-[640px]">
```
por:
```jsx
{/* DESKTOP: tabela */}
<div className="hidden md:block rounded-3xl border border-border bg-card shadow-card overflow-hidden">
  <div className="overflow-x-auto">
    <table className="w-full min-w-[640px]">
```

(mantém o restante do `<table>` idêntico — cabeçalho, `tbody`, paginação — apenas fechando essa `div` externa antes do bloco de cards abaixo, no lugar onde hoje fecha em `</div>` seguido do `<ModalConfirmacao>`, linha ~354).

- [ ] **Step 2: Adicionar a view em cards para mobile**

Criar um novo componente `CardEstudioMobile` no mesmo arquivo, logo antes de `export default function TabelaEstudios` (depois de `SkeletonLinha`, linha ~199):

```jsx
function CardEstudioMobile({ estudio, onSuspender, onReativar, onAcessar, acessando, acessarDesabilitado }) {
  const ativo = estudio.status !== 'suspenso';

  return (
    <div className="rounded-2xl border border-border bg-card shadow-card p-4">
      <div className="flex items-start justify-between gap-2">
        <div className="flex items-center gap-3 min-w-0">
          <div className="w-9 h-9 rounded-xl bg-primary-soft flex items-center justify-center shrink-0">
            <span className="text-primary font-black text-sm">{inicial(estudio)}</span>
          </div>
          <div className="min-w-0">
            <p className="font-bold text-sm text-foreground truncate">{nomeExibicao(estudio)}</p>
            <p className="text-xs text-muted-foreground font-mono truncate">{estudio.slug ?? '—'}</p>
          </div>
        </div>
        <MenuAcoes
          estudio={estudio}
          onSuspender={onSuspender}
          onReativar={onReativar}
          onAcessar={onAcessar}
          acessando={acessando}
          acessarDesabilitado={acessarDesabilitado}
        />
      </div>

      <div className="mt-3 flex flex-wrap items-center gap-x-4 gap-y-1.5 text-xs text-muted-foreground">
        <span className="flex items-center gap-1">
          <Users size={13} /> {contagem(estudio.total_alunos)} alunos
        </span>
        <span className="flex items-center gap-1">
          <GraduationCap size={13} /> {contagem(estudio.total_professores)} professores
        </span>
        <span className="flex items-center gap-1">
          <Calendar size={13} /> {formatarData(estudio.criado_em)}
        </span>
      </div>

      <div className="mt-3">
        <Badge tone={ativo ? 'success' : 'warning'} variant="soft">
          {ativo ? 'Ativo' : 'Suspenso'}
        </Badge>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Renderizar os cards no bloco mobile**

Logo depois de fechar a `div` da tabela desktop (fim do Step 1, antes do bloco de paginação — que também deve ficar só em desktop), adicionar:

```jsx
{/* MOBILE: cards */}
<div className="md:hidden space-y-3">
  {isLoading
    ? [...Array(3)].map((_, i) => (
        <div key={i} className="rounded-2xl border border-border bg-card p-4 space-y-3">
          <Skeleton className="h-4 w-32" />
          <Skeleton className="h-3 w-full" />
        </div>
      ))
    : estudios.length === 0
    ? (
      <EmptyState
        icon={<Building2 size={28} />}
        title={buscaDebounced ? 'Nenhum estúdio encontrado' : 'Nenhum estúdio cadastrado'}
        description={
          buscaDebounced
            ? `Nenhum resultado para "${buscaDebounced}".`
            : 'Crie o primeiro estúdio usando o botão acima.'
        }
      />
    )
    : estudios.map((e) => (
      <CardEstudioMobile
        key={e.id}
        estudio={e}
        onSuspender={(est) => setConfirmacao({ estudio: est, acao: 'suspender' })}
        onReativar={(est)  => setConfirmacao({ estudio: est, acao: 'reativar'  })}
        onAcessar={handleAcessar}
        acessando={acessandoId === e.id && impersonando}
        acessarDesabilitado={impersonando}
      />
    ))
  }

  {!isLoading && estudios.length > 0 && (
    <p className="text-xs text-muted-foreground font-medium px-1">
      {totalCount} estúdio{totalCount !== 1 ? 's' : ''}
      {buscaDebounced && ` encontrado${totalCount !== 1 ? 's' : ''} para "${buscaDebounced}"`}
    </p>
  )}

  {!isLoading && totalPaginas > 1 && (
    <div className="flex items-center justify-center gap-3 pt-1">
      <button
        className="p-1.5 rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground transition-colors disabled:opacity-30 disabled:hover:bg-transparent"
        disabled={pagina === 0 || isPlaceholderData}
        onClick={() => setPagina((p) => Math.max(0, p - 1))}
        aria-label="Pagina anterior"
      >
        <ChevronLeft size={16} />
      </button>
      <span className="text-xs font-bold text-foreground tabular-nums">
        {pagina + 1} / {totalPaginas}
      </span>
      <button
        className="p-1.5 rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground transition-colors disabled:opacity-30 disabled:hover:bg-transparent"
        disabled={pagina + 1 >= totalPaginas || isPlaceholderData}
        onClick={() => setPagina((p) => p + 1)}
        aria-label="Proxima pagina"
      >
        <ChevronRight size={16} />
      </button>
    </div>
  )}
</div>
```

- [ ] **Step 4: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: sem regressões.

- [ ] **Step 5: Verificar manualmente no browser em viewport mobile**

Rodar `npm run dev`, abrir `/super/estudios`, emular viewport 375×812. Esperado: nenhum scroll horizontal, cards empilhados verticalmente mostrando nome/slug, alunos, professores, data e status, com o menu de Ações acessível (⋯) em cada card. Redimensionar para desktop (≥768px): tabela original deve voltar a aparecer, cards mobile somem.

- [ ] **Step 6: Commit**

```bash
git add webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx
git commit -m "fix(super-admin): tabela de estudios vira cards empilhados em mobile (PED-93)"
```

---

### Task 5: PED-94 — Item de menu "Professors" em inglês

**Files:**
- Modify: `webapp/src/lib/terminologia.js:59-71` (corrigir pluralização)
- Create: `webapp/src/lib/terminologia.test.js`

**Interfaces:**
- Produces: `resolverRotuloPlural(chave, ctx)` mantém a mesma assinatura — só o algoritmo interno de pluralização muda (de "sempre sufixo 's'" para uma regra pt-BR mínima cobrindo o dicionário atual).

**Root cause:** o menu (`src/components/Sidebar.jsx:63`) já usa `t.rotuloPlural('professor')`, então não há string "Professors" hardcoded em lugar nenhum — o bug é que `resolverRotuloPlural` faz `${resolverRotulo(...)}s`, e "Professor" + "s" = "Professors", que por coincidência forma uma palavra em inglês. A correção é no algoritmo de pluralização, não uma string fixa.

- [ ] **Step 1: Escrever o teste que falha**

Criar `webapp/src/lib/terminologia.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { resolverRotuloPlural } from './terminologia';

describe('resolverRotuloPlural', () => {
  it('pluraliza "professor" corretamente em pt-BR (não "Professors")', () => {
    // Regressão (PED-94): sufixo naive 's' produzia "Professors" (parece
    // inglês) em vez de "Professores" no menu lateral do painel do estúdio.
    expect(resolverRotuloPlural('professor', { segmento: 'danca_fitness' })).toBe('Professores');
  });

  it('pluraliza "treinador" corretamente em pt-BR (segmento escolinha_esportiva)', () => {
    expect(resolverRotuloPlural('professor', { segmento: 'escolinha_esportiva' })).toBe('Treinadores');
  });

  it('mantém pluralização simples para palavras terminadas em vogal', () => {
    expect(resolverRotuloPlural('aluno', { segmento: 'danca_fitness' })).toBe('Alunos');
    expect(resolverRotuloPlural('modalidade', { segmento: 'danca_fitness' })).toBe('Modalidades');
  });

  it('respeita terminologia customizada do tenant, pluralizando o rótulo customizado', () => {
    expect(
      resolverRotuloPlural('professor', { terminologia: { professor: 'Mentor' }, segmento: 'danca_fitness' })
    ).toBe('Mentores');
  });
});
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

Run: `npm test -- terminologia.test.js`
Expected: FAIL — `resolverRotuloPlural('professor', ...)` retorna `'Professors'`/`'Treinadors'`/`'Mentors'`, não `'Professores'`/`'Treinadores'`/`'Mentores'`.

- [ ] **Step 3: Corrigir `resolverRotuloPlural`**

Em `webapp/src/lib/terminologia.js`, substituir (linhas 59-71):

```js
/**
 * Plural simples (só sufixo 's') — cobre os casos atuais do dicionário.
 * Se algum rótulo custom precisar de plural irregular no futuro, a via é
 * o próprio tenant digitar o plural certo num campo `_plural` dedicado;
 * não complicar agora por um caso hipotético.
 *
 * @param {string} chave
 * @param {{ terminologia?: Record<string,string>, segmento?: string }} ctx
 * @returns {string}
 */
export function resolverRotuloPlural(chave, ctx) {
  return `${resolverRotulo(chave, ctx)}s`;
}
```

por:

```js
/**
 * Plural regular em pt-BR — cobre os casos atuais do dicionário (palavras
 * terminadas em vogal, e em 'r'/'s'/'z', que é o padrão de "Professor" →
 * "Professores"). Corrige regressão (PED-94) em que o sufixo naive 's'
 * produzia "Professors" (lê como inglês) em vez de "Professores".
 * Se algum rótulo custom precisar de plural irregular no futuro (ex.:
 * terminado em 'ão'), a via é o próprio tenant digitar o plural certo num
 * campo `_plural` dedicado; não complicar agora por um caso hipotético.
 *
 * @param {string} chave
 * @param {{ terminologia?: Record<string,string>, segmento?: string }} ctx
 * @returns {string}
 */
export function resolverRotuloPlural(chave, ctx) {
  const singular = resolverRotulo(chave, ctx);
  const ultima = singular.charAt(singular.length - 1).toLowerCase();
  if (ultima === 'r' || ultima === 's' || ultima === 'z') return `${singular}es`;
  return `${singular}s`;
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Run: `npm test -- terminologia.test.js`
Expected: PASS (4 testes)

- [ ] **Step 5: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: sem regressões (nenhum outro consumidor de `resolverRotuloPlural` depende do sufixo naive — confirmar com `grep -rn "rotuloPlural" src` antes de rodar, só pra registrar os call sites tocados).

- [ ] **Step 6: Verificar manualmente no browser**

Como super-admin, "Acessar como admin" em qualquer estúdio, olhar o menu lateral "Gestão e Operação": o item deve mostrar "Professores".

- [ ] **Step 7: Commit**

```bash
git add webapp/src/lib/terminologia.js webapp/src/lib/terminologia.test.js
git commit -m "fix(terminologia): corrige pluralizacao pt-BR de 'professor' no menu (PED-94)"
```

---

### Task 6: PED-95 — Card de "Plano Livre" mostra "999" em vez de "Ilimitado"

**Files:**
- Modify: `webapp/src/pages/Planos.jsx:360`

- [ ] **Step 1: Corrigir a exibição do card resumido**

Em `webapp/src/pages/Planos.jsx`, trocar (linha 360):
```jsx
<Calendar size={12} /> {plano.frequencia_semanal}
```
por:
```jsx
<Calendar size={12} />
{plano.frequencia_semanal === LIMITE_ILIMITADO_SEMANA ? 'Ilimitado' : plano.frequencia_semanal}
```

(`LIMITE_ILIMITADO_SEMANA` já está importado no topo do arquivo, linha 14 — mesma constante usada em `regra.limite === LIMITE_ILIMITADO_SEMANA ? 'Ilimitado (Livre)' : ...` no modal de edição, linha 564.)

- [ ] **Step 2: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: sem regressões.

- [ ] **Step 3: Verificar manualmente no browser**

Como super-admin, "Acessar como admin" no estúdio "Espaço Iluminus", ir em Planos. O card "Plano Livre" deve mostrar "Ilimitado" ao lado do ícone de calendário em vez de "999". Um plano normal (não ilimitado, ex. "3x na semana") deve continuar mostrando o número normalmente.

- [ ] **Step 4: Commit**

```bash
git add webapp/src/pages/Planos.jsx
git commit -m "fix(planos): card resumido mostra 'Ilimitado' em vez do valor sentinela 999 (PED-95)"
```

---

### Task 7: PED-96 — Página Financeiro não responsiva em mobile

**Files:**
- Modify: `webapp/src/pages/Financeiro.jsx:506,515`
- Modify: `webapp/src/pages/SuperAdmin/components/BannerImpersonation.jsx:43,51`

- [ ] **Step 1: Empilhar os botões do cabeçalho em telas estreitas**

Em `webapp/src/pages/Financeiro.jsx`, trocar (linha 515):
```jsx
<div className="flex gap-2 w-full md:w-auto">
```
por:
```jsx
<div className="flex flex-col sm:flex-row gap-2 w-full md:w-auto">
```

E ajustar o padding do container raiz da página para o mesmo padrão responsivo já usado em `src/pages/Dashboard.jsx:210` (que se adapta bem ao mobile) — trocar (linha 506):
```jsx
<div className="p-8 space-y-8 animate-in fade-in max-w-7xl mx-auto">
```
por:
```jsx
<div className="p-6 md:p-8 space-y-8 animate-in fade-in max-w-7xl mx-auto">
```

- [ ] **Step 2: Corrigir o truncamento do banner de impersonation em telas estreitas**

Em `webapp/src/pages/SuperAdmin/components/BannerImpersonation.jsx`, trocar (linha 43-46):
```jsx
<div
  role="alert"
  aria-live="polite"
  className="fixed top-0 inset-x-0 z-[200] flex items-center justify-between gap-3 px-4 py-2 bg-warning text-warning-foreground shadow-lg"
>
```
por:
```jsx
<div
  role="alert"
  aria-live="polite"
  className="fixed top-0 inset-x-0 z-[200] flex flex-col sm:flex-row sm:items-center justify-between gap-2 sm:gap-3 px-4 py-2 bg-warning text-warning-foreground shadow-lg"
>
```

E trocar (linha 51) — remove o `truncate` (que cortava o texto sem dar espaço pro botão) já que agora o botão vai para a própria linha em telas estreitas:
```jsx
<p className="text-sm font-bold truncate">
```
por:
```jsx
<p className="text-sm font-bold">
```

- [ ] **Step 3: Rodar suíte completa e lint**

Run: `npm test && npm run lint`
Expected: sem regressões.

- [ ] **Step 4: Verificar manualmente no browser em viewport mobile**

Como super-admin, "Acessar como admin" no estúdio "Espaço Iluminus", ir em Financeiro > Mensalidades, emular viewport 375×812. Esperado: nenhum scroll horizontal na página inteira; os dois botões do cabeçalho ("Criar mensalidades de [mês]" e "Novo Lançamento") aparecem empilhados, cada um ocupando a largura toda e totalmente visível; o banner laranja "Visualizando como admin de Espaço Iluminus" mostra o nome completo (quebrando para uma segunda linha se necessário) com o botão "Sair da visualização" visível abaixo, sem cortar texto.

- [ ] **Step 5: Commit**

```bash
git add webapp/src/pages/Financeiro.jsx webapp/src/pages/SuperAdmin/components/BannerImpersonation.jsx
git commit -m "fix(financeiro): remove overflow horizontal e truncamento do banner em mobile (PED-96)"
```

---

### Task 8: Abrir a Pull Request

**Files:** nenhum (apenas comandos git/gh)

- [ ] **Step 1: Push da branch**

```bash
git push -u origin worktree-ped-90-96-bug-fixes
```

- [ ] **Step 2: Criar a PR**

```bash
gh pr create --title "fix: corrige PED-90 a PED-96 (validacao, i18n e responsividade no Super Admin e painel do estudio)" --body "$(cat <<'EOF'
## Summary
- PED-90: valida formato de WhatsApp ao criar estudio (Super Admin)
- PED-91: detecta slug duplicado em tempo real ao criar estudio
- PED-92: corrige acentuacao na tabela, modais e toasts de Estudios
- PED-93: tabela de Estudios vira cards empilhados em mobile
- PED-94: corrige pluralizacao pt-BR de "professor" no menu lateral (nao mais "Professors")
- PED-95: card de plano mostra "Ilimitado" em vez do valor sentinela 999
- PED-96: remove overflow horizontal e truncamento do banner de impersonation em mobile na pagina Financeiro

## Test plan
- [ ] `npm test` (webapp) — suite completa verde
- [ ] `npm run lint` (webapp) — sem erros novos
- [ ] Manual: `/super/estudios/novo` — campo WhatsApp valida formato; slug duplicado mostra erro em tempo real
- [ ] Manual: `/super/estudios` — acentuacao correta, layout mobile em cards (375x812)
- [ ] Manual: painel do estudio — menu lateral mostra "Professores"; card de Plano Livre mostra "Ilimitado"; `/financeiro` sem overflow horizontal em mobile (375x812)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 3: Reportar a URL da PR ao usuário.**

---

## Self-Review

**Spec coverage:** PED-90 → Task 1. PED-91 → Task 2. PED-92 → Task 3. PED-93 → Task 4. PED-94 → Task 5. PED-95 → Task 6. PED-96 → Task 7. PR final → Task 8. Todos os 7 tickets cobertos, um por task, mais a task de entrega.

**Placeholder scan:** nenhum "TBD"/"implementar depois" — cada step tem código completo ou comando exato a rodar.

**Type consistency:** `validarTelefone(telefone: string) => boolean` usado de forma consistente entre Task 1 (implementação) e o uso em `handleWhatsappChange`/`handleSubmit`. `verificarSlugDisponivel(slug: string) => Promise<boolean>` consistente entre Task 2 (service) e o `useQuery` que o consome. `resolverRotuloPlural(chave, ctx) => string` mantém assinatura idêntica à já usada em `Sidebar.jsx` e `useTerminologia.js` — nenhum call site precisa mudar.
