# PED-105 — Trial de 14 dias — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar de verdade a promessa de "14 dias grátis" da landing: todo estúdio criado via self-service ganha um prazo de 14 dias armazenado no banco, perde o acesso quando o prazo expira (reaproveitando a tela `EstudioBloqueado.jsx`), e o admin do estúdio vê quantos dias restam num banner discreto.

**Architecture:** Uma coluna nova `estudios.trial_ends_at` (nullable — `NULL` = sem prazo) é preenchida pela RPC `criar_estudio_transacional` só no fluxo self-service. O enforcement de verdade acontece no banco: `estudio_id_atual()` — a function central que quase toda RLS policy usa pra resolver "qual é o estúdio atual" — passa a exigir também que o trial não tenha expirado, então um estúdio com trial vencido perde acesso aos dados mesmo via chamada direta à API, não só via redirect de frontend. `verificar_status_estudio()` informa o frontend do motivo do bloqueio (status vs. trial) pra render a mensagem certa. Um banner novo, visível só pro admin, mostra os dias restantes.

**Tech Stack:** Supabase (Postgres + RLS + RPC via `plpgsql`/`sql`), Deno Edge Functions, React 19 + Vite, Vitest.

**Spec:** [docs/superpowers/specs/2026-09-01-trial-14-dias-design.md](../specs/2026-09-01-trial-14-dias-design.md)

## Global Constraints

- Toda migration é aditiva (`ALTER TABLE ADD COLUMN` nullable, `CREATE OR REPLACE`/`DROP+CREATE FUNCTION` — nunca `DROP COLUMN`/`DROP TABLE` de algo em uso) — ver `docs/DEPLOY.md` seção 1.
- Toda migration é aplicada e validada em **staging** (`qjmybxkfjkxttggdjxga`) antes de qualquer cogitação de produção (`tciiepqmnrrcjnqhspvw`) — nunca aplicar direto em produção. Confirme o project ref certo via `list_projects`/`get_project` antes de aplicar qualquer coisa (múltiplos projetos Supabase existem nesta conta).
- Toda migration que cria/altera `estudio_id_atual()` ou qualquer function usada em RLS policy segue `supabase/RLS_MIGRATION_CHECKLIST.md`: simulação de 2 tenants + `anon` (com `begin; set local role ...; ...; rollback;`, nunca commit) e `get_advisors` depois de aplicar.
- Toda migration que faz `DROP FUNCTION` + `CREATE FUNCTION` (mudança de shape de retorno) reaplica explicitamente os `GRANT`/`REVOKE` na mesma migration — `DROP`+`CREATE` reseta o ACL pro default do Postgres (ver checklist, incidente PED-83).
- Estúdios já existentes ficam com `trial_ends_at = NULL` (isentos — decisão do brainstorming, sem migration de backfill).
- Estúdios criados pelo super_admin (`criar-estudio`) nascem com `trial_ends_at = NULL` (sem prazo) — só o self-service (`criar-meu-estudio`) inicia o trial.
- Sem cron/job de varredura — a expiração é avaliada sob demanda em `verificar_status_estudio()`, chamada já existente no carregamento de sessão.
- Sem captura de cartão nem upgrade self-service — isso é o [PED-115](https://linear.app/pedro-schuster/issue/PED-115/cobranca-automatica-pos-trial-plano-pago-self-service-com-cartao), fora de escopo aqui.

---

## File Structure

- **Create** `supabase/migrations/20260901120000_add_trial_ends_at_estudios.sql` — coluna `trial_ends_at` em `estudios`.
- **Create** `supabase/migrations/20260901121000_criar_estudio_transacional_trial.sql` — `criar_estudio_transacional` ganha `p_iniciar_trial`.
- **Modify** `supabase/functions/criar-estudio/index.ts` — passa `p_iniciar_trial: false` (onboarding manual do super_admin).
- **Create** `supabase/migrations/20260901122000_trial_enforcement.sql` — `estudio_id_atual()` + `verificar_status_estudio()` (enforcement real + motivo do bloqueio).
- **Create** `webapp/src/lib/trial.js` — `diasRestantesTrial()` e `chaveMensagemBloqueio()`, funções puras testáveis.
- **Create** `webapp/src/lib/trial.test.js` — testes das duas funções acima.
- **Modify** `webapp/src/pages/EstudioBloqueado.jsx` — branch de mensagem para `trial_expirado`.
- **Create** `webapp/src/components/shared/TrialBanner.jsx` — banner de dias restantes, só para `perfil === 'admin'`.
- **Modify** `webapp/src/App.jsx` — monta `<TrialBanner />` dentro de `LayoutComSidebar`.
- **Create** `supabase/migrations/20260901123000_listar_estudios_admin_trial.sql` — expõe `trial_ends_at` na listagem do super_admin.
- **Modify** `webapp/src/services/superAdminService.js` — `removerTrialEstudio(estudioId)`.
- **Modify** `webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx` — badge de trial + ação "Remover trial" no menu.
- **Test:** `webapp/src/lib/trial.test.js` (vitest, lógica pura) + simulação SQL de 2 tenants em staging via `execute_sql` (RLS_MIGRATION_CHECKLIST) para a Task 3 — este repo não testa componentes React renderizados (nenhum `@testing-library/react` instalado; convenção existente é extrair lógica pura pra `lib/*.test.js`, ver `rotaModulo.js`/`rotaModulo.test.js`), então `EstudioBloqueado.jsx`/`TrialBanner.jsx`/`TabelaEstudios.jsx` não ganham teste de render — só a lógica que extraímos pra `trial.js`.

---

### Task 1: Trial começa na criação do estúdio (self-service)

**Files:**
- Create: `supabase/migrations/20260901120000_add_trial_ends_at_estudios.sql`
- Create: `supabase/migrations/20260901121000_criar_estudio_transacional_trial.sql`

**Interfaces:**
- Produces: coluna `public.estudios.trial_ends_at timestamptz` (nullable).
- Produces: `criar_estudio_transacional(p_nome text, p_slug text, p_whatsapp text, p_instagram text, p_admin_id uuid, p_admin_nome text, p_admin_email text, p_iniciar_trial boolean DEFAULT true)` — parâmetro novo no final, com default `true` (self-service não precisa passar nada).

- [ ] **Step 1: Confirmar o project ref de staging antes de aplicar qualquer coisa**

Rode `list_projects` (MCP Supabase) e confirme que existe um projeto cujo nome indica "staging" com ref `qjmybxkfjkxttggdjxga` (conforme `docs/superpowers/plans/2026-08-26-ped20-23-banco-rls-auditoria.md`) — não assuma o ref sem checar, a conta tem múltiplos projetos Supabase.

- [ ] **Step 2: Criar a migration da coluna**

`supabase/migrations/20260901120000_add_trial_ends_at_estudios.sql`:
```sql
-- Trial de 14 dias (PED-105): guarda quando o trial do estúdio termina.
-- NULL = sem prazo (estúdio já existente antes da feature, ou criado
-- manualmente pelo super_admin via onboarding comercial — ver
-- criar_estudio_transacional, migration seguinte).
ALTER TABLE public.estudios
  ADD COLUMN trial_ends_at timestamptz;
```

- [ ] **Step 3: Criar a migration da RPC**

`supabase/migrations/20260901121000_criar_estudio_transacional_trial.sql`:
```sql
-- PED-105: criar_estudio_transacional ganha p_iniciar_trial (default true).
-- Self-service (criar-meu-estudio) chama sem passar o parâmetro — ganha
-- 14 dias. Onboarding manual do super_admin (criar-estudio) passa
-- p_iniciar_trial => false — nasce sem prazo (acordo comercial à parte).
--
-- CREATE OR REPLACE adicionando parâmetro novo com DEFAULT no final
-- preserva o OID da function e, com ele, o GRANT/REVOKE existentes — mas
-- reaplicamos explicitamente por segurança (mesmo espírito do
-- RLS_MIGRATION_CHECKLIST sobre não confiar cegamente no ACL implícito).
CREATE OR REPLACE FUNCTION public.criar_estudio_transacional(p_nome text, p_slug text, p_whatsapp text, p_instagram text, p_admin_id uuid, p_admin_nome text, p_admin_email text, p_iniciar_trial boolean DEFAULT true)
 RETURNS json
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
DECLARE
  v_estudio_id UUID;
BEGIN

  -- ── 1. CRIAR ESTÚDIO ───────────────────────────────────────────────────────
  INSERT INTO public.estudios (nome, slug, whatsapp, instagram, trial_ends_at)
  VALUES (
    p_nome, p_slug, p_whatsapp, p_instagram,
    CASE WHEN p_iniciar_trial THEN now() + interval '14 days' ELSE NULL END
  )
  RETURNING id INTO v_estudio_id;

  -- ── 2. VINCULAR ADMIN AO ESTÚDIO ──────────────────────────────────────────
  INSERT INTO public.estudio_membros (estudio_id, user_id, role)
  VALUES (v_estudio_id, p_admin_id, 'admin');

  -- ── 3. PROVISIONAR CONFIGURAÇÕES DE REPASSE PADRÃO ────────────────────────
  INSERT INTO public.configuracoes_repasse (
    estudio_id,
    valor_1_modalidade,
    valor_multi_modalidade,
    plano_livre_pct_prof,
    plano_livre_pct_casa,
    aula_avulsa_valor,
    aula_avulsa_pct_prof,
    aula_avulsa_pct_casa,
    aula_experimental_valor,
    aula_experimental_pct_prof
  )
  VALUES (
    v_estudio_id,
    0, 0, 0, 100,
    0, 0, 100,
    0, 0
  );

  -- ── RETORNO ────────────────────────────────────────────────────────────────
  RETURN json_build_object(
    'estudio_id', v_estudio_id,
    'estudio_nome', p_nome,
    'estudio_slug', p_slug
  );

END;
$function$
;

GRANT EXECUTE ON FUNCTION public.criar_estudio_transacional(p_nome text, p_slug text, p_whatsapp text, p_instagram text, p_admin_id uuid, p_admin_nome text, p_admin_email text, p_iniciar_trial boolean) TO service_role;
REVOKE EXECUTE ON FUNCTION public.criar_estudio_transacional(p_nome text, p_slug text, p_whatsapp text, p_instagram text, p_admin_id uuid, p_admin_nome text, p_admin_email text, p_iniciar_trial boolean) FROM public, anon, authenticated;
```

- [ ] **Step 4: Aplicar as duas migrations em staging e validar**

Use a MCP tool `apply_migration` (Supabase) contra o project ref de staging confirmado no Step 1, uma migration por vez, na ordem (coluna primeiro, depois a function — a function referencia a coluna).

Depois de aplicar, rode via `execute_sql` (staging):
```sql
select column_name, data_type, is_nullable
from information_schema.columns
where table_schema = 'public' and table_name = 'estudios' and column_name = 'trial_ends_at';
```
Expected: 1 linha, `data_type = 'timestamp with time zone'`, `is_nullable = 'YES'`.

```sql
select proacl from pg_proc where proname = 'criar_estudio_transacional';
```
Expected: ACL mostra `service_role` com `X` (execute) e **não** mostra `anon`/`authenticated`/`public` soltos — confirma que o `GRANT`/`REVOKE` explícito do Step 3 funcionou (ver `RLS_MIGRATION_CHECKLIST.md` — "verifique o ACL real, não o que a migration diz").

```sql
select criar_estudio_transacional('QA Trial Teste', 'qa-trial-teste-' || extract(epoch from now())::int, null, null, gen_random_uuid(), 'QA', 'qa@teste.com');
```
Expected: retorna o `json` com `estudio_id`. Confirme o trial:
```sql
select trial_ends_at from estudios where id = '<estudio_id retornado acima>';
```
Expected: `trial_ends_at` ≈ `now() + 14 days`. Depois, apague a linha de teste (`delete from estudios where id = '<estudio_id>'` — cascata cuida de `estudio_membros`/`configuracoes_repasse`, é um estúdio isolado sem dados reais).

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260901120000_add_trial_ends_at_estudios.sql supabase/migrations/20260901121000_criar_estudio_transacional_trial.sql
git commit -m "feat(trial): estudios.trial_ends_at + criar_estudio_transacional inicia trial de 14 dias (PED-105)"
```

---

### Task 2: Onboarding manual do super_admin não entra em trial

**Files:**
- Modify: `supabase/functions/criar-estudio/index.ts:154-161`

**Interfaces:**
- Consumes: `criar_estudio_transacional(..., p_iniciar_trial)` do Task 1.

- [ ] **Step 1: Passar `p_iniciar_trial: false` na chamada da RPC**

Em `supabase/functions/criar-estudio/index.ts`, o bloco atual (linhas 154-161):
```ts
  const { data: rpcData, error: rpcErr } = await admin.rpc('criar_estudio_transacional', {
    p_nome:        nome.trim(),
    p_slug:        slugNorm,
    p_whatsapp:    whatsapp?.trim() ?? null,
    p_instagram:   instagram?.trim() ?? null,
    p_admin_id:    adminAuthId,
    p_admin_nome:  adminNome.trim(),
    p_admin_email: adminEmail,
  });
```
passa a:
```ts
  const { data: rpcData, error: rpcErr } = await admin.rpc('criar_estudio_transacional', {
    p_nome:          nome.trim(),
    p_slug:          slugNorm,
    p_whatsapp:      whatsapp?.trim() ?? null,
    p_instagram:     instagram?.trim() ?? null,
    p_admin_id:      adminAuthId,
    p_admin_nome:    adminNome.trim(),
    p_admin_email:   adminEmail,
    // PED-105: onboarding manual do super_admin não inicia o trial de
    // 14 dias — presume-se acordo comercial fora do fluxo self-service.
    p_iniciar_trial: false,
  });
```

Não mexer em `supabase/functions/criar-meu-estudio/index.ts` — o self-service continua sem passar o parâmetro, usando o default `true` da RPC.

- [ ] **Step 2: Deploy da function em staging e teste manual**

```bash
supabase functions deploy criar-estudio --project-ref qjmybxkfjkxttggdjxga
```
Use o painel Super Admin (`/super/estudios/novo`) contra staging, ou chame a function diretamente com um payload de teste, e confirme via `execute_sql`:
```sql
select trial_ends_at from estudios where slug = '<slug-do-estudio-de-teste-criado>';
```
Expected: `trial_ends_at IS NULL`.

- [ ] **Step 3: Commit**

```bash
git add supabase/functions/criar-estudio/index.ts
git commit -m "feat(trial): onboarding manual do super_admin não inicia trial (PED-105)"
```

---

### Task 3: Enforcement real no banco (RLS) + motivo do bloqueio pro frontend

**Files:**
- Create: `supabase/migrations/20260901122000_trial_enforcement.sql`

**Interfaces:**
- Produces: `estudio_id_atual()` — mesma assinatura (`RETURNS uuid`), agora também exige `trial_ends_at is null or trial_ends_at > now()` no branch não-impersonado.
- Produces: `verificar_status_estudio()` — novo shape de retorno `TABLE(estudio_id uuid, nome text, status text, trial_ends_at timestamptz, motivo_bloqueio text, bloqueado boolean)`. `motivo_bloqueio` é `'status'`, `'trial_expirado'` ou `null`.
- Consumes: nada de tasks anteriores (a coluna `trial_ends_at` já existe desde o Task 1).

Este é o ponto mais sensível do plano — `estudio_id_atual()` é usada por praticamente toda RLS policy do banco (isolamento entre tenants depende dela). Siga `supabase/RLS_MIGRATION_CHECKLIST.md` à risca.

- [ ] **Step 1: Criar a migration**

`supabase/migrations/20260901122000_trial_enforcement.sql`:
```sql
-- PED-105: enforcement do trial de 14 dias.
--
-- estudio_id_atual() ganha uma condição extra: um estúdio com trial
-- expirado deixa de resolver como "estúdio atual" pro membro comum
-- (não-impersonado), igual já acontece hoje pra status <> 'ativo'. Como é
-- CREATE OR REPLACE com a MESMA assinatura (sem parâmetro novo, sem
-- mudança de retorno), o OID e os GRANTs existentes são preservados —
-- toda RLS policy que depende desta function herda o filtro
-- automaticamente, sem precisar tocar em policy nenhuma.
--
-- status continua obrigatoriamente 'ativo' durante o trial (a própria
-- condição de baixo já exige isso) — a expiração do trial é rastreada só
-- por trial_ends_at, nunca mudando o enum de status.
CREATE OR REPLACE FUNCTION public.estudio_id_atual()
 RETURNS uuid
 LANGUAGE sql
 STABLE SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
  select coalesce(
    public.estudio_ativo_via_override(),
    (
      select em.estudio_id
      from estudio_membros em
      join estudios e on e.id = em.estudio_id
      where em.user_id = auth.uid()
        and e.status = 'ativo'
        and (e.trial_ends_at is null or e.trial_ends_at > now())
      limit 1
    )
  );
$function$
;

-- verificar_status_estudio() muda de shape de retorno (colunas novas
-- trial_ends_at/motivo_bloqueio) — RETURNS TABLE diferente exige DROP +
-- CREATE (CREATE OR REPLACE não permite mudar tipo de retorno). Por isso
-- os GRANTs são reaplicados explicitamente logo abaixo (ver
-- RLS_MIGRATION_CHECKLIST.md, seção "DROP FUNCTION + CREATE FUNCTION
-- apaga os GRANT/REVOKE" — foi exatamente esse o incidente da PED-83).
DROP FUNCTION public.verificar_status_estudio();

CREATE FUNCTION public.verificar_status_estudio()
 RETURNS TABLE(estudio_id uuid, nome text, status text, trial_ends_at timestamptz, motivo_bloqueio text, bloqueado boolean)
 LANGUAGE sql
 STABLE SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
  select
    e.id,
    e.nome,
    e.status,
    e.trial_ends_at,
    case
      when e.status <> 'ativo' then 'status'
      when e.trial_ends_at is not null and e.trial_ends_at < now() then 'trial_expirado'
      else null
    end as motivo_bloqueio,
    (e.status <> 'ativo' or (e.trial_ends_at is not null and e.trial_ends_at < now())) as bloqueado
  from estudio_membros em
  join estudios e on e.id = em.estudio_id
  where em.user_id = auth.uid()
  order by em.created_at asc
  limit 1;
$function$
;

GRANT EXECUTE ON FUNCTION public.verificar_status_estudio() TO authenticated;
GRANT EXECUTE ON FUNCTION public.verificar_status_estudio() TO service_role;
REVOKE EXECUTE ON FUNCTION public.verificar_status_estudio() FROM public, anon;
```

- [ ] **Step 2: Aplicar em staging**

`apply_migration` (MCP Supabase) contra o ref de staging confirmado no Task 1.

- [ ] **Step 3: Simulação de 2 tenants (RLS_MIGRATION_CHECKLIST) — ANTES de forçar qualquer trial expirado**

Rode via `execute_sql` (staging), pra confirmar que a mudança em `estudio_id_atual()` não quebrou o isolamento entre tenants nem regrediu nenhum estúdio saudável:
```sql
select em.user_id, em.estudio_id, em.role, e.trial_ends_at
from estudio_membros em
join estudios e on e.id = em.estudio_id
where em.role = 'admin'
order by em.estudio_id
limit 10;
```
Escolha 2 `user_id` de `estudio_id` diferentes (idealmente com `trial_ends_at is null`, pra isolar "isso ainda funciona pra quem não está em trial" do teste de expiração que vem no Step 4). Para cada um:
```sql
begin;
set local role authenticated;
set local request.jwt.claims = '{"sub": "<uuid-tenant-a>", "role": "authenticated"}';
select estudio_id_atual();  -- confirme que resolve pro estúdio A
select * from estudios where id = estudio_id_atual();  -- 1 linha, o estúdio A
rollback;
```
Repita trocando pelo `user_id`/estúdio B. **Nenhum dos dois pode ver linha do outro.** Rode também o caso `anon`:
```sql
begin;
set local role anon;
select estudio_id_atual();  -- deve vir null
rollback;
```

- [ ] **Step 4: Teste de expiração de trial de verdade**

O estúdio de teste do Task 1 já foi apagado no fim daquele task — crie um novo do zero, iniciando o trial desta vez:
```sql
select criar_estudio_transacional('QA Trial Expirado', 'qa-trial-expirado-' || extract(epoch from now())::int, null, null, gen_random_uuid(), 'QA', 'qa2@teste.com', true);
```
Pegue o `estudio_id` retornado e o `user_id` do admin vinculado (`select user_id from estudio_membros where estudio_id = '<estudio_id>'`), force a expiração e confirme o bloqueio:
```sql
update estudios set trial_ends_at = now() - interval '1 day' where id = '<estudio-de-teste>';

begin;
set local role authenticated;
set local request.jwt.claims = '{"sub": "<user_id-do-admin-de-teste>", "role": "authenticated"}';
select estudio_id_atual();  -- ESPERADO: null (trial expirado bloqueia)
select * from verificar_status_estudio();  -- ESPERADO: bloqueado = true, motivo_bloqueio = 'trial_expirado'
rollback;
```
Depois, apague o estúdio de teste (`delete from estudios where id = '<estudio-de-teste>'`).

- [ ] **Step 5: `get_advisors` (MCP Supabase, tipo `security`) contra staging**

Expected: nenhum advisor novo relacionado a `estudio_id_atual`/`verificar_status_estudio` (ex.: `rls_enabled_no_policy`, `*_security_definer_function_executable` pra `anon`). Se aparecer algo novo relacionado a essas 2 functions, pare e corrija antes de seguir — não é esperado, já que os GRANT/REVOKE foram reaplicados no Step 1.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260901122000_trial_enforcement.sql
git commit -m "feat(trial): estudio_id_atual() e verificar_status_estudio() aplicam bloqueio real por trial expirado (PED-105)"
```

---

### Task 4: Frontend — banner de dias restantes + tela de bloqueio

**Files:**
- Create: `webapp/src/lib/trial.js`
- Create: `webapp/src/lib/trial.test.js`
- Modify: `webapp/src/pages/EstudioBloqueado.jsx`
- Create: `webapp/src/components/shared/TrialBanner.jsx`
- Modify: `webapp/src/App.jsx`

**Interfaces:**
- Consumes: `estudioStatusInfo` de `useAuth()` (já carrega `trial_ends_at`/`motivo_bloqueio` automaticamente a partir do Task 3 — `useAuth.jsx` só repassa o retorno de `verificar_status_estudio()`, sem precisar de nenhuma mudança nele).
- Produces: `diasRestantesTrial(trialEndsAt, agora = new Date())` → `number | null`. `chaveMensagemBloqueio(statusInfo)` → `string | null`.

- [ ] **Step 1: Escrever os testes de `webapp/src/lib/trial.js` (falhando)**

`webapp/src/lib/trial.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { diasRestantesTrial, chaveMensagemBloqueio } from './trial';

describe('diasRestantesTrial', () => {
  it('retorna null quando não há trial_ends_at', () => {
    expect(diasRestantesTrial(null)).toBeNull();
    expect(diasRestantesTrial(undefined)).toBeNull();
  });

  it('arredonda pra cima os dias restantes até o fim do trial', () => {
    const agora = new Date('2026-09-01T12:00:00Z');
    const fimEm3DiasE1Hora = new Date('2026-09-04T13:00:00Z').toISOString();
    expect(diasRestantesTrial(fimEm3DiasE1Hora, agora)).toBe(4);
  });

  it('retorna 0 no último dia (fim ainda não passou)', () => {
    const agora = new Date('2026-09-01T12:00:00Z');
    const fimDaquiA6Horas = new Date('2026-09-01T18:00:00Z').toISOString();
    expect(diasRestantesTrial(fimDaquiA6Horas, agora)).toBe(1);
  });

  it('retorna negativo quando o trial já expirou', () => {
    const agora = new Date('2026-09-05T12:00:00Z');
    const fimNoPassado = new Date('2026-09-01T12:00:00Z').toISOString();
    expect(diasRestantesTrial(fimNoPassado, agora)).toBe(-4);
  });
});

describe('chaveMensagemBloqueio', () => {
  it('retorna null quando não há statusInfo', () => {
    expect(chaveMensagemBloqueio(null)).toBeNull();
    expect(chaveMensagemBloqueio(undefined)).toBeNull();
  });

  it('retorna "trial_expirado" quando motivo_bloqueio indica trial', () => {
    expect(chaveMensagemBloqueio({ status: 'ativo', motivo_bloqueio: 'trial_expirado' })).toBe('trial_expirado');
  });

  it('retorna o status quando o bloqueio não é por trial', () => {
    expect(chaveMensagemBloqueio({ status: 'suspenso', motivo_bloqueio: 'status' })).toBe('suspenso');
    expect(chaveMensagemBloqueio({ status: 'inativo', motivo_bloqueio: 'status' })).toBe('inativo');
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Rodar dentro de `webapp/`:
```bash
npx vitest run src/lib/trial.test.js
```
Expected: FAIL — `Cannot find module './trial'` (o arquivo `trial.js` ainda não existe).

- [ ] **Step 3: Implementar `webapp/src/lib/trial.js`**

```js
// webapp/src/lib/trial.js
//
// Lógica pura do trial de 14 dias (PED-105) — extraída pra ser testável
// sem precisar renderizar componente nenhum (este projeto não usa
// @testing-library/react; convenção aqui é lib pura + component "burro"
// que só consome, ver rotaModulo.js).

export function diasRestantesTrial(trialEndsAt, agora = new Date()) {
  if (!trialEndsAt) return null;
  const fim = new Date(trialEndsAt);
  const diffMs = fim.getTime() - agora.getTime();
  return Math.ceil(diffMs / (1000 * 60 * 60 * 24));
}

export function chaveMensagemBloqueio(statusInfo) {
  if (!statusInfo) return null;
  if (statusInfo.motivo_bloqueio === 'trial_expirado') return 'trial_expirado';
  return statusInfo.status;
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

```bash
npx vitest run src/lib/trial.test.js
```
Expected: PASS, 6 testes.

- [ ] **Step 5: Adicionar o branch de mensagem em `EstudioBloqueado.jsx`**

Em `webapp/src/pages/EstudioBloqueado.jsx`, importar o helper:
```jsx
import { chaveMensagemBloqueio } from '../lib/trial';
```
Adicionar a entrada nova em `MENSAGENS_POR_STATUS` (linha 19-35 atual):
```jsx
const MENSAGENS_POR_STATUS = {
  inativo: {
    titulo: 'Estúdio inativo',
    descricao:
      'O acesso a este estúdio está temporariamente pausado. Se você acredita que isso é um engano, entre em contato com o suporte.',
  },
  suspenso: {
    titulo: 'Estúdio suspenso',
    descricao:
      'O acesso a este estúdio foi suspenso pela administração da plataforma. Entre em contato com o suporte para regularizar a situação.',
  },
  cancelado: {
    titulo: 'Estúdio encerrado',
    descricao:
      'Este estúdio foi encerrado e não está mais disponível. Entre em contato com o suporte se precisar de mais informações.',
  },
  trial_expirado: {
    titulo: 'Período de teste encerrado',
    descricao:
      'Seus 14 dias de teste grátis chegaram ao fim. Entre em contato com o suporte para continuar usando a Nexofy.',
  },
};
```
Trocar a resolução da mensagem (linha 46-48 atual):
```jsx
  const status = estudioStatusInfo?.status;
  const nomeEstudio = estudioStatusInfo?.nome ?? 'seu estúdio';
  const { titulo, descricao } = MENSAGENS_POR_STATUS[status] ?? MENSAGEM_PADRAO;
```
por:
```jsx
  const nomeEstudio = estudioStatusInfo?.nome ?? 'seu estúdio';
  const chave = chaveMensagemBloqueio(estudioStatusInfo);
  const { titulo, descricao } = MENSAGENS_POR_STATUS[chave] ?? MENSAGEM_PADRAO;
```

- [ ] **Step 6: Criar `webapp/src/components/shared/TrialBanner.jsx`**

```jsx
// webapp/src/components/shared/TrialBanner.jsx
//
// Banner discreto com os dias restantes do trial de 14 dias (PED-105),
// visível do dia 1 ao dia 14. Só aparece pro admin do estúdio — é quem
// decide sobre upgrade, professores/alunos não precisam ver. Fica em
// fluxo normal (não fixed): nunca coexiste com BannerImpersonation, já
// que impersonation é sempre perfil 'super_admin', nunca 'admin'.

import React from 'react';
import { Clock } from 'lucide-react';
import { useAuth } from '../../hooks/useAuth';
import { diasRestantesTrial } from '../../lib/trial';

const LIMITE_DIAS_URGENTE = 3;

export default function TrialBanner() {
  const { perfil, estudioStatusInfo } = useAuth();

  if (perfil !== 'admin') return null;

  const dias = diasRestantesTrial(estudioStatusInfo?.trial_ends_at);
  if (dias === null || dias < 0) return null;

  const urgente = dias <= LIMITE_DIAS_URGENTE;

  return (
    <div
      role="status"
      className={`flex items-center justify-center gap-2 px-4 py-2 text-sm font-bold shrink-0 text-center ${
        urgente ? 'bg-warning text-warning-foreground' : 'bg-info-soft text-info'
      }`}
    >
      <Clock size={15} className="shrink-0" />
      <span>
        Período de teste — {dias === 0 ? 'último dia' : `faltam ${dias} dia${dias === 1 ? '' : 's'}`}
      </span>
    </div>
  );
}
```

- [ ] **Step 7: Montar o banner em `App.jsx`**

Adicionar o import (perto dos outros imports de `components/shared`, linha ~22):
```jsx
import TrialBanner from './components/shared/TrialBanner';
```
Em `LayoutComSidebar` (linha 121 atual), adicionar `<TrialBanner />` como primeiro filho da coluna de conteúdo:
```jsx
      <div className="flex-1 flex flex-col h-screen overflow-hidden w-full max-w-full">
        <TrialBanner />

        <div className="md:hidden flex items-center justify-between bg-card border-b border-border p-4 shrink-0 z-10 shadow-sm transition-colors duration-300">
```

- [ ] **Step 8: Rodar a suíte inteira + lint + build**

```bash
npm test
npm run lint
npm run build
```
Expected: todos os testes passam (105 + 6 novos = 111), sem erro novo de lint/build.

- [ ] **Step 9: Commit**

```bash
git add webapp/src/lib/trial.js webapp/src/lib/trial.test.js webapp/src/pages/EstudioBloqueado.jsx webapp/src/components/shared/TrialBanner.jsx webapp/src/App.jsx
git commit -m "feat(trial): banner de dias restantes + tela de bloqueio por trial expirado (PED-105)"
```

---

### Task 5: Alavanca manual do super_admin — "Remover trial"

**Files:**
- Create: `supabase/migrations/20260901123000_listar_estudios_admin_trial.sql`
- Modify: `webapp/src/services/superAdminService.js`
- Modify: `webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx`

**Interfaces:**
- Consumes: `diasRestantesTrial` de `webapp/src/lib/trial.js` (Task 4).
- Produces: `superAdminService.removerTrialEstudio(estudioId)` — `Promise<void>`, usado só dentro de `TabelaEstudios.jsx`.

- [ ] **Step 1: Criar e aplicar a migration em staging**

`supabase/migrations/20260901123000_listar_estudios_admin_trial.sql`:
```sql
-- PED-105: expõe trial_ends_at na listagem de estúdios do super_admin,
-- pra alimentar a badge de "dias restantes" em TabelaEstudios.jsx. Muda
-- o shape de RETURNS TABLE -> exige DROP + CREATE (ver
-- RLS_MIGRATION_CHECKLIST.md sobre reaplicar GRANT/REVOKE).
DROP FUNCTION public.listar_estudios_admin(integer, integer, text);

CREATE FUNCTION public.listar_estudios_admin(p_limit integer, p_offset integer, p_busca text DEFAULT NULL::text)
 RETURNS TABLE(id uuid, nome text, slug text, whatsapp text, instagram text, criado_em timestamp with time zone, status text, trial_ends_at timestamp with time zone, total_alunos bigint, total_professores bigint, total_count bigint)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
begin
  if not eh_super_admin() then
    raise exception 'access denied' using errcode = '42501';
  end if;

  return query
  select
    e.id, e.nome, e.slug, e.whatsapp, e.instagram,
    e.created_at                     as criado_em,
    e.status,
    e.trial_ends_at,
    coalesce(a.total_alunos, 0)      as total_alunos,
    coalesce(p.total_professores, 0) as total_professores,
    count(*) over ()                 as total_count
  from estudios e
  left join (select estudio_id, count(*) as total_alunos from alunos group by estudio_id) a
    on a.estudio_id = e.id
  left join (select estudio_id, count(*) as total_professores from professores group by estudio_id) p
    on p.estudio_id = e.id
  where p_busca is null
     or e.nome ilike '%' || p_busca || '%'
     or e.slug ilike '%' || p_busca || '%'
  order by e.created_at desc
  limit p_limit offset p_offset;
end;
$function$
;

GRANT EXECUTE ON FUNCTION public.listar_estudios_admin(p_limit integer, p_offset integer, p_busca text) TO authenticated;
GRANT EXECUTE ON FUNCTION public.listar_estudios_admin(p_limit integer, p_offset integer, p_busca text) TO service_role;
REVOKE EXECUTE ON FUNCTION public.listar_estudios_admin(p_limit integer, p_offset integer, p_busca text) FROM public, anon;
```

Aplicar via `apply_migration` (MCP Supabase, staging), depois validar:
```sql
select proacl from pg_proc where proname = 'listar_estudios_admin';
```
Expected: `authenticated` e `service_role` no ACL, sem `anon`/`public` soltos.
```sql
select * from listar_estudios_admin(5, 0, null);
```
Expected: retorna linhas com a coluna `trial_ends_at` presente (pode ser `null` pros estúdios existentes, isso é esperado).

Nota: esta function não é usada por RLS policy nenhuma (só chamada direto pelo painel super_admin) — não precisa da simulação de 2 tenants do checklist, só a validação de ACL/retorno acima.

- [ ] **Step 2: Commit da migration**

```bash
git add supabase/migrations/20260901123000_listar_estudios_admin_trial.sql
git commit -m "feat(trial): listar_estudios_admin expõe trial_ends_at (PED-105)"
```

- [ ] **Step 3: Adicionar `removerTrialEstudio` em `superAdminService.js`**

Em `webapp/src/services/superAdminService.js`, logo depois de `alterarStatusEstudio` (linha 87 atual), seguindo o MESMO padrão (update direto via `.from()`, apoiado na policy `"tenant: update proprio estudio"` que já libera `eh_super_admin()` — sem precisar de RPC nova):
```js
/**
 * Remove o prazo de trial de um estúdio (trial_ends_at = NULL) — alavanca
 * manual do super_admin enquanto não existe cobrança automática (PED-115).
 * Mesmo padrão de alterarStatusEstudio: update direto via RLS, que já
 * libera super_admin pra alterar qualquer estúdio.
 */
async function removerTrialEstudio(estudioId) {
  const { error } = await supabase
    .from('estudios')
    .update({ trial_ends_at: null })
    .eq('id', estudioId);

  if (error) throw error;
}
```
E adicionar ao objeto exportado no final do arquivo (linha 127-134 atual):
```js
export const superAdminService = {
  listarEstudios,
  metricasGlobais,
  saudeSistema,
  alterarStatusEstudio,
  removerTrialEstudio,
  criarEstudio,
  verificarSlugDisponivel,
};
```

- [ ] **Step 4: Badge de trial + ação "Remover trial" em `TabelaEstudios.jsx`**

Import novo, junto aos existentes (linha 29-38 atual, adicionar `Clock` aos ícones do `lucide-react` e o helper):
```jsx
import {
  Building2, Users, GraduationCap, MoreHorizontal,
  Pause, Play, Eye, Calendar, Clock, Loader2, ChevronLeft, ChevronRight,
} from 'lucide-react';
```
```jsx
import { diasRestantesTrial } from '../../../lib/trial';
```

Helper novo, logo depois de `contagem()` (linha 60-62 atual):
```jsx
// Rótulo + tom da badge de trial: sem trial (isento), expirado, ou dias restantes.
function rotuloTrial(estudio) {
  const dias = diasRestantesTrial(estudio.trial_ends_at);
  if (dias === null) return { texto: 'Sem trial', tone: 'neutral' };
  if (dias < 0) return { texto: 'Trial expirado', tone: 'destructive' };
  return { texto: `${dias} dia${dias === 1 ? '' : 's'}`, tone: dias <= 3 ? 'warning' : 'info' };
}
```

Em `MenuAcoes` (linha 65-117 atual), adicionar prop `onRemoverTrial` e o botão novo — trocar a assinatura:
```jsx
function MenuAcoes({ estudio, onSuspender, onReativar, onRemoverTrial, onAcessar, acessando, acessarDesabilitado }) {
```
e, dentro do menu, logo antes do bloco `{ativo ? (...) : (...)}` de suspender/reativar (depois do `<div className="my-1 border-t border-border" />` na linha 95):
```jsx
            {estudio.trial_ends_at && (
              <button
                className="flex w-full items-center gap-2.5 px-4 py-2.5 text-sm font-bold text-info hover:bg-info-soft transition-colors"
                onClick={() => { setAberto(false); onRemoverTrial(estudio); }}
              >
                <Clock size={15} /> Remover trial
              </button>
            )}

```

Em `LinhaEstudio` (linha 119-177 atual): adicionar prop `onRemoverTrial`, repassar pra `MenuAcoes`, e adicionar a célula da badge entre a de "Status" e a de "Ações":
```jsx
function LinhaEstudio({ estudio, onSuspender, onReativar, onRemoverTrial, onAcessar, acessando, acessarDesabilitado }) {
```
```jsx
      <td className="py-4 px-4">
        <Badge tone={ativo ? 'success' : 'warning'} variant="soft">
          {ativo ? 'Ativo' : 'Suspenso'}
        </Badge>
      </td>

      <td className="py-4 px-4">
        {(() => {
          const { texto, tone } = rotuloTrial(estudio);
          return <Badge tone={tone} variant="soft">{texto}</Badge>;
        })()}
      </td>

      <td className="py-4 pr-6 pl-4 text-right">
        <MenuAcoes
          estudio={estudio}
          onSuspender={onSuspender}
          onReativar={onReativar}
          onRemoverTrial={onRemoverTrial}
          onAcessar={onAcessar}
          acessando={acessando}
          acessarDesabilitado={acessarDesabilitado}
        />
      </td>
```
(remove a célula de Ações antiga duplicada — ela já existia, só ganhou `onRemoverTrial` na chamada de `MenuAcoes`; a célula da badge é a única coisa nova entre Status e Ações.)

No cabeçalho da tabela (linha 319-327 atual), adicionar a coluna:
```jsx
                <TH className="pl-6 pr-4 text-left">Estúdio</TH>
                <TH className="px-4 text-center">Alunos</TH>
                <TH className="px-4 text-center">Professores</TH>
                <TH className="px-4 text-left">Criado em</TH>
                <TH className="px-4 text-left">Status</TH>
                <TH className="px-4 text-left">Trial</TH>
                <TH className="pr-6 pl-4 text-right">Ações</TH>
```
E atualizar `colSpan={6}` do `EmptyState` (linha 336) e `[...Array(4)]` do `SkeletonLinha` (linha 191) pra `colSpan={7}` e `[...Array(5)]` respectivamente, já que agora são 7 colunas.

Em `CardEstudioMobile` (linha 201-245 atual): adicionar prop `onRemoverTrial`, repassar pra `MenuAcoes`, e mostrar a badge de trial junto da badge de status existente (linha 238-242):
```jsx
function CardEstudioMobile({ estudio, onSuspender, onReativar, onRemoverTrial, onAcessar, acessando, acessarDesabilitado }) {
```
```jsx
      <div className="mt-3 flex items-center gap-2 flex-wrap">
        <Badge tone={ativo ? 'success' : 'warning'} variant="soft">
          {ativo ? 'Ativo' : 'Suspenso'}
        </Badge>
        {(() => {
          const { texto, tone } = rotuloTrial(estudio);
          return <Badge tone={tone} variant="soft">{texto}</Badge>;
        })()}
      </div>
```
E repassar `onRemoverTrial` pro `<MenuAcoes .../>` dentro dele (linha 216-223 atual).

No componente principal `TabelaEstudios` (linha 247 em diante): adicionar a mutation, o handler, e repassar `onRemoverTrial` nas duas renderizações (desktop `LinhaEstudio` e mobile `CardEstudioMobile`).

Logo depois da mutation `alterarStatus` (linha 279-287 atual):
```jsx
  const { mutate: removerTrial, isPending: isRemovendoTrial } = useMutation({
    mutationFn: ({ id }) => superAdminService.removerTrialEstudio(id),
    onSuccess: () => {
      showToast.success('Trial removido — estúdio sem prazo.');
      qc.invalidateQueries({ queryKey: ['super-admin'] });
    },
    onError:   (err) => showToast.error(err.message || 'Erro ao remover trial.'),
    onSettled: ()    => setConfirmacao(null),
  });
```

Nas duas chamadas de `<LinhaEstudio .../>` e `<CardEstudioMobile .../>` (linha 350-364 e 424-434 atuais), adicionar:
```jsx
                    onRemoverTrial={(est) => setConfirmacao({ estudio: est, acao: 'removerTrial' })}
```

E trocar o `<ModalConfirmacao .../>` final (linha 469-485 atual) pra lidar com as 3 ações:
```jsx
      <ModalConfirmacao
        aberto={!!confirmacao}
        fechar={() => setConfirmacao(null)}
        onConfirm={() => {
          if (confirmacao.acao === 'removerTrial') {
            removerTrial({ id: confirmacao.estudio.id });
          } else {
            alterarStatus({
              id:     confirmacao.estudio.id,
              status: confirmacao.acao === 'suspender' ? 'suspenso' : 'ativo',
            });
          }
        }}
        loading={isAlterando || isRemovendoTrial}
        tipo={
          confirmacao?.acao === 'suspender'    ? 'warning' :
          confirmacao?.acao === 'removerTrial' ? 'info'    :
          'success'
        }
        titulo={
          confirmacao?.acao === 'suspender'    ? 'Suspender estúdio?' :
          confirmacao?.acao === 'removerTrial' ? 'Remover trial?'     :
          'Reativar estúdio?'
        }
        mensagem={
          confirmacao?.acao === 'suspender'
            ? `O estúdio "${confirmacao?.estudio?.nome}" perderá acesso ao sistema.`
            : confirmacao?.acao === 'removerTrial'
            ? `O estúdio "${confirmacao?.estudio?.nome}" deixa de ter prazo de trial — acesso liberado indefinidamente até você tomar outra ação.`
            : `O estúdio "${confirmacao?.estudio?.nome}" voltará a ter acesso normalmente.`
        }
        textoConfirmar={
          confirmacao?.acao === 'suspender'    ? 'Suspender'     :
          confirmacao?.acao === 'removerTrial' ? 'Remover trial' :
          'Reativar'
        }
      />
```

- [ ] **Step 5: Rodar a suíte + lint + build**

```bash
npm test
npm run lint
npm run build
```
Expected: sem erro novo.

- [ ] **Step 6: Commit**

```bash
git add webapp/src/services/superAdminService.js webapp/src/pages/SuperAdmin/components/TabelaEstudios.jsx
git commit -m "feat(trial): acao 'Remover trial' no painel super_admin (PED-105)"
```

---

### Task 6: Validação manual ponta-a-ponta em staging + PR

**Files:** nenhum arquivo novo — só validação e abertura do PR.

- [ ] **Step 1: Passagem manual completa em staging**

Contra o front-end apontado pra staging (`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` de staging — ver `README.md` seção "Como rodar localmente"):

1. Cadastro self-service novo (`/cadastro` → `/cadastro/estudio`) → confirme que o estúdio criado tem `trial_ends_at` ≈ 14 dias no futuro (`select trial_ends_at from estudios where slug = '<slug>'`).
2. Logue como admin desse estúdio → confirme que o `TrialBanner` aparece no topo do painel com "faltam 14 dias" (ou 13, dependendo do arredondamento).
3. Via `execute_sql`, force a expiração: `update estudios set trial_ends_at = now() - interval '1 hour' where id = '<estudio-de-teste>'`.
4. Recarregue a página (a checagem é lazy, no carregamento de sessão) → confirme redirect pra `/estudio-bloqueado` com a mensagem "Período de teste encerrado".
5. No painel `/super/estudios` (como super_admin), ache esse estúdio → confirme a badge "Trial expirado" → clique "Remover trial" → confirme o toast de sucesso e que a badge vira "Sem trial".
6. Logue de novo como admin do estúdio de teste → confirme que o acesso voltou ao normal (sem redirect, sem banner).
7. Apague o estúdio de teste (`delete from estudios where id = '<estudio-de-teste>'`).

- [ ] **Step 2: Push da branch e abertura do PR**

```bash
git push -u origin worktree-ped-105-trial-14-dias
```
```bash
export PATH="$PATH:/c/Program Files/GitHub CLI"
gh pr create --title "feat(trial): implementa trial de 14 dias (PED-105)" --body "$(cat <<'EOF'
## Summary
- `estudios.trial_ends_at` (nullable) guarda o fim do trial; estúdios existentes ficam isentos (NULL).
- `criar_estudio_transacional` ganha `p_iniciar_trial` (default true) — self-service inicia o trial, onboarding manual do super_admin (`criar-estudio`) não.
- Enforcement real no banco: `estudio_id_atual()` passa a considerar `trial_ends_at` — um estúdio com trial expirado perde acesso aos dados via RLS, não só via redirect de frontend. `verificar_status_estudio()` informa o motivo do bloqueio (`status` vs `trial_expirado`).
- Frontend: `EstudioBloqueado.jsx` ganha mensagem específica pra trial expirado; novo `TrialBanner` mostra dias restantes pro admin do estúdio, do dia 1 ao 14.
- Painel super_admin: badge de trial por estúdio + ação "Remover trial" (alavanca manual até existir cobrança automática — [PED-115](https://linear.app/pedro-schuster/issue/PED-115/cobranca-automatica-pos-trial-plano-pago-self-service-com-cartao)).
- Todas as migrations já aplicadas e validadas em staging (`qjmybxkfjkxttggdjxga`), incluindo simulação de isolamento entre 2 tenants + `get_advisors` (`estudio_id_atual()` é usada por quase toda RLS policy do banco).

## Test plan
- [x] `npm test` — 111 testes (105 existentes + 6 novos de `trial.js`)
- [x] `npm run lint` / `npm run build` — sem erro novo
- [x] Simulação de 2 tenants + anon em staging (RLS_MIGRATION_CHECKLIST) — isolamento preservado
- [x] `get_advisors` (staging) — sem achado novo
- [x] Passagem manual ponta-a-ponta em staging: cadastro → banner → forçar expiração → tela de bloqueio → "Remover trial" no super_admin → acesso normalizado
- [ ] Migration aplicada em produção (`tciiepqmnrrcjnqhspvw`) — **pendente, decisão do Pedro**: aplicar produção + deploy da function `criar-estudio` antes de mergear (ver `docs/DEPLOY.md` seção 1 — ordem migration → function → frontend), já que o frontend deste PR passa a depender de `trial_ends_at`/`motivo_bloqueio` existirem.

Closes PED-105

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 3: Atualizar o ticket no Linear**

Mover PED-105 para "In Review" e linkar o PR.

- [ ] **Step 4: Sinalizar a aplicação em produção pro Pedro**

Não aplicar as migrations em produção nem mergear o PR autonomamente — são mudanças de RLS/enforcement em banco de produção compartilhado. Depois do PR aberto, avisar explicitamente que produção (`tciiepqmnrrcjnqhspvw`) ainda não recebeu as 4 migrations, e que a ordem seguindo `docs/DEPLOY.md` é: (1) aplicar as 4 migrations em produção, (2) deploy de `criar-estudio` em produção, (3) só então mergear/promover o frontend — pra nenhum usuário bater num frontend novo (que já espera `trial_ends_at`) contra um banco de produção ainda sem a coluna.
